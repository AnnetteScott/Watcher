import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.annette.mediaer',
  appName: 'Mediaer',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
